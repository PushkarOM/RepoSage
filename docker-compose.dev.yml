services:
  api:
    volumes:
      - ./backend/app:/app/app
      - ./backend/eval:/app/eval
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir app

  worker:
    volumes:
      - ./backend/app:/app/app
  